#2.8 keywords
''' 33 ta..... false class finally is
    return none continue for lambda try 
    true def from nonlocal while and
    sel global not with as elif if or
    yield assert else import pass break 
    except in raise '''
print("keywords") 